
str title = hello_world()

func header = (bool input) str {
  if input == true {
    return  "bye world"
  }

  return "hi world"
}

str term = $`echo $TERM`

html {
  head > title > @{title}
  body {
    h1 > @{header(true)}
    div > @{term}
  }
}
