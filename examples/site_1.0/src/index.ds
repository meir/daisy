
str title = hello_world()

func header = (str input) element {
  return :h1#title.red.center {
    @{input}
  }
}

element head = header("Hello World")

str term = $`echo $TERM`

html {
  head > title > @{title}
  body {
    h1 > @{head}
    div > @{term}
  }
}
