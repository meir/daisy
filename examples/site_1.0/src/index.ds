
str title = hello_world()

func header = (str input) str {
  return : h1#title.red.center {
    @{input}
  }
}

str head = header("Hello world") + "!"

html {
  head > title > @{title}
  body > @{head}
}
